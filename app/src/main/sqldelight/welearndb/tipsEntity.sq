CREATE TABLE tipsEntity (
 id INTEGER PRIMARY KEY NOT NULL,
 type TEXT,
 tip TEXT
);

insertTip:
INSERT OR REPLACE INTO tipsEntity
VALUES (?,?,?);


getTipsByType:

SELECT *
FROM tipsEntity
WHERE type=?;